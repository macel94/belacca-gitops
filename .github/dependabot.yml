version: 2
updates:
  # GitHub Actions used by the GitOps validation workflow.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily
    groups:
      github-actions:
        patterns:
          - "*"
