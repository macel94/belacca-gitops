{
  "dashboard": {
    "id": null,
    "title": "Belacca Platform Reliability (staged)",
    "schemaVersion": 39,
    "tags": ["belacca", "sre", "proposed"],
    "timezone": "utc",
    "refresh": "30s",
    "panels": [
      {"title": "Pong HTTP request rate", "type": "timeseries", "query": "sum(rate(pong_http_requests_total[5m]))"},
      {"title": "Pong HTTP failure rate", "type": "timeseries", "query": "sum(rate(pong_http_requests_failure_total[5m]))"},
      {"title": "Active rooms", "type": "stat", "query": "pong_rooms_active"},
      {"title": "Waiting rooms", "type": "stat", "query": "pong_rooms_waiting"},
      {"title": "Playing rooms", "type": "stat", "query": "pong_rooms_playing"},
      {"title": "Active WebSockets", "type": "stat", "query": "pong_websockets_active"},
      {"title": "Flux Ready-condition failures", "type": "timeseries", "query": "sum(rate(gotk_reconcile_condition{status=\"False\",type=\"Ready\"}[5m]))"}
    ],
    "notes": "Queries are intentionally label-light. Add synthetic probe panels only after an external publisher supplies sanitized metrics. Do not expose this dashboard publicly."
  }
}
